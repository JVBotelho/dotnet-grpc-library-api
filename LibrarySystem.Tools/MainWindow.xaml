<Window x:Class="LibrarySystem.Tools.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:b="clr-namespace:LibrarySystem.Tools.Behaviors"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:LibrarySystem.Tools.ViewModels"
        mc:Ignorable="d"
        Title="LibrarySystem - Service Graph Inspector" Height="600" Width="1100"
        Background="#1E1E1E" Foreground="White">

    <d:Window.DataContext>
        <vm:MainViewModel />
    </d:Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" /> <RowDefinition Height="2*" />    <RowDefinition Height="Auto" /> <RowDefinition Height="1*" /> </Grid.RowDefinitions>

        <Border Background="#252526" Padding="10" BorderBrush="#3F3F46" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="SERVICE INSPECTOR" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,20,0"
                           Foreground="#007ACC" />

                <Button Command="{Binding LoadGraphDataCommand}"
                        Content="⚡ Refresh Topology"
                        Padding="10,5" Background="#3E3E42" Foreground="White" BorderThickness="0" Cursor="Hand"/>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />     <ColumnDefinition Width="Auto" />  </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                          PanningMode="Both">
                <Grid Width="3000" Height="3000">
                    <Grid.Background>
                        <DrawingBrush TileMode="Tile" Viewport="0,0,50,50" ViewportUnits="Absolute" Opacity="0.1">
                            <DrawingBrush.Drawing>
                                <GeometryDrawing>
                                    <GeometryDrawing.Pen>
                                        <Pen Brush="Gray" Thickness="1" />
                                    </GeometryDrawing.Pen>
                                    <GeometryDrawing.Geometry>
                                        <RectangleGeometry Rect="0,0,50,50" />
                                    </GeometryDrawing.Geometry>
                                </GeometryDrawing>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Grid.Background>

                    <ItemsControl ItemsSource="{Binding Edges}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Line X1="{Binding X1}" Y1="{Binding Y1}"
                                      X2="{Binding X2}" Y2="{Binding Y2}"
                                      Stroke="#444444" StrokeThickness="2"
                                      IsHitTestVisible="False" />
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <ItemsControl ItemsSource="{Binding Nodes}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <Canvas />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemContainerStyle>
                            <Style TargetType="ContentPresenter">
                                <Setter Property="Canvas.Left" Value="{Binding X}" />
                                <Setter Property="Canvas.Top" Value="{Binding Y}" />
                            </Style>
                        </ItemsControl.ItemContainerStyle>
                        
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Width="200" Height="100" CornerRadius="4" 
                                        Background="#2D2D30">
                                    
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Setter Property="BorderBrush" Value="#3F3F46"/>
                                            <Setter Property="BorderThickness" Value="1"/>
                                            <Setter Property="Effect">
                                                <Setter.Value>
                                                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                                                </Setter.Value>
                                            </Setter>
                                            
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                                                    <Setter Property="BorderBrush" Value="White"/>
                                                    <Setter Property="BorderThickness" Value="2"/>
                                                    <Setter Property="Effect">
                                                        <Setter.Value>
                                                            <DropShadowEffect Color="White" BlurRadius="15" Opacity="0.4" ShadowDepth="0"/>
                                                        </Setter.Value>
                                                    </Setter>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>

                                    <i:Interaction.Behaviors>
                                        <b:NodeDragBehavior />
                                    </i:Interaction.Behaviors>

                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="*" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>

                                        <Border Background="#007ACC" Height="4" CornerRadius="4,4,0,0" />

                                        <StackPanel Grid.Row="1" Margin="10">
                                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"
                                                       TextTrimming="CharacterEllipsis" Foreground="#F1F1F1" />
                                            <TextBlock Text="{Binding Subtitle}" FontSize="12" Foreground="#CCCCCC"
                                                       Margin="0,4,0,0" />
                                        </StackPanel>

                                        <TextBlock Grid.Row="2" Text="{Binding Id, StringFormat='ID: {0}'}"
                                                   FontSize="10" Foreground="#666666" HorizontalAlignment="Right"
                                                   Margin="5" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>

            <Border Grid.Column="1" Width="300" Background="#252526" BorderBrush="#3F3F46" BorderThickness="1,0,0,0">
                <StackPanel Margin="15" DataContext="{Binding Inspector}">
                    
                    <TextBlock Text="PROPERTIES" FontWeight="Bold" Foreground="#007ACC" Margin="0,0,0,15"/>

                    <StackPanel>
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedNode}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>

                        <TextBlock Text="Title" Foreground="#AAAAAA" Margin="0,5"/>
                        <TextBox Text="{Binding EditTitle, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="#333337" Foreground="White" BorderThickness="0"/>

                        <TextBlock Text="Author" Foreground="#AAAAAA" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding EditAuthor, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="#333337" Foreground="White" BorderThickness="0"/>

                        <TextBlock Text="Year" Foreground="#AAAAAA" Margin="0,10,0,5"/>
                        <TextBox Text="{Binding EditYear, UpdateSourceTrigger=PropertyChanged}" Padding="8" Background="#333337" Foreground="White" BorderThickness="0"/>

                        <Button Command="{Binding SaveChangesCommand}" 
                                Content="💾 Save Changes (gRPC)" 
                                Margin="0,20,0,0" Padding="10" 
                                Background="#007ACC" Foreground="White" BorderThickness="0" Cursor="Hand" FontWeight="Bold"/>
                        
                        <TextBlock Text="Changes are sent directly to the Core Service via gRPC and persisted in PostgreSQL." 
                                   TextWrapping="Wrap" FontSize="11" Foreground="Gray" Margin="0,10" FontStyle="Italic"/>
                    </StackPanel>
                    
                    <TextBlock Text="Select a node to edit details." Foreground="Gray" FontStyle="Italic" 
                               HorizontalAlignment="Center" Margin="0,50">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding SelectedNode}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                </StackPanel>
            </Border>
        </Grid>

        <Border Grid.Row="2" Background="#0F0F0F" BorderBrush="#333" BorderThickness="0,1,0,0">
            <Grid DataContext="{Binding WafLogs}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

                <Border Background="#1E1E1E" Padding="10,5">
                    <Grid>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="SECURITY CONSOLE (WAF)" Foreground="#CCC" FontWeight="Bold" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding StatusText}" Margin="15,0,0,0" VerticalAlignment="Center" 
                                       FontSize="11" Foreground="#00FF00" FontFamily="Consolas"/>
                        </StackPanel>
                        
                        <ToggleButton IsChecked="{Binding IsMonitoring}" 
                                      Content="{Binding MonitoringButtonText}" 
                                      HorizontalAlignment="Right" Width="120" Height="24"
                                      Background="#333" Foreground="White" BorderThickness="0"/>
                         </Grid>
                </Border>

                <ListBox Grid.Row="1" ItemsSource="{Binding Logs}" 
                         Background="#0F0F0F" BorderThickness="0" 
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>  <ColumnDefinition Width="100"/> <ColumnDefinition Width="*"/>   </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Timestamp}" Foreground="#666" FontFamily="Consolas" FontSize="11"/>
                                
                                <TextBlock Grid.Column="1" Text="{Binding Action}" FontWeight="Bold" FontFamily="Consolas" FontSize="11">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#00FF00"/> <Style.Triggers>
                                                <DataTrigger Binding="{Binding Action}" Value="Blocked/Alert">
                                                    <Setter Property="Foreground" Value="#FF3333"/> </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>

                                <TextBlock Grid.Column="2" Text="{Binding Details}" Foreground="#DDD" FontFamily="Consolas" FontSize="11" TextTrimming="CharacterEllipsis"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</Window>